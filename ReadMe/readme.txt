let cors=require("cors") ---> cors'u arastir.
const dotenv= require("dotenv"); ---> dotenv arastir.
let bodyParser=require("body-parser"); ---> bodyParser arastir.
let port=process.env.port; // ---> nodeJs.proccess arastir 